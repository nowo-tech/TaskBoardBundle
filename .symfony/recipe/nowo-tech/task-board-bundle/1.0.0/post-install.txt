TaskBoardBundle installed.

Recommended (rich task descriptions):
  composer require nowo-tech/tag-input-bundle
  composer require nowo-tech/tiptap-editor-bundle

Optional (time tracking):
  composer require nowo-tech/time-track-bundle
  # then configure nowo_time_track task_provider and team_context_provider

Next steps:
  1. Configure security access_control for /tools/task-board → ROLE_USER
  2. Run doctrine migrations or schema update for task_board_* tables
  3. php bin/console assets:install public (TagInputBundle + TiptapEditorBundle assets)
  4. Optional: implement TaskBoardAccessCheckerInterface or listen to TaskBoardEvents for team ACL

See docs/INSTALLATION.md and docs/SPEC-DRIVEN-DEVELOPMENT.md.
